#include<stdio.h>

/**
 * main - Entery point
 *
 * Description: prints exactly the string "with proper grammar,
 * but the outcome is a piece of art,"
 * Return: returns 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
