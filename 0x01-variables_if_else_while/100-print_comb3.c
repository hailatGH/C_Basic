#include <stdio.h>

/**
 * main - Entry point
 *
 * Description: prints all possible combinations of two digit numbers
 * by using only putchar.
 * Return: returns 0
 */
int main(void)
{
	putchar(1 + '0');
	return (0);
}
